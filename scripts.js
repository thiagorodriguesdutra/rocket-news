const input = document.querySelector('input');

/* Simples função que exibe um alert com o valor digitado no input */
addEventListener('submit', () => {
  alert('O email digitado é ' + input.value)
})

/* Salvando os emails em um banco de dados */

// code...